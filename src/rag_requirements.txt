# Optional requirements for enhanced RAG system functionality
# Install with: pip install -r src/rag_requirements.txt

# Vector store and embeddings (for enhanced pattern matching)
chromadb>=0.4.0
sentence-transformers>=2.2.0

# Clustering and advanced analytics
scikit-learn>=1.3.0

# Interactive mapping (required for Folium maps)
folium>=0.14.0

# Note: Core RAG functionality works without these dependencies
# but some features will be disabled with fallback implementations